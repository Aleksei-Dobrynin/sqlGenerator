namespace Domain.Entities
{
    public class {{ entity_name }}
    {
{{~ for column in columns ~}}
{{~ if column.IsNullable && !column.IsPrimaryKey && !(column.CSharpType | string.ends_with "?") ~}}
        public {{ column.CSharpType }}? {{ column.Name }} { get; set; }
{{~ else ~}}
        public {{ column.CSharpType }} {{ column.Name }} { get; set; }
{{~ end ~}}
{{~ end ~}}
    }
}
